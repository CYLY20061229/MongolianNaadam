<view class="page">
<view class="profile-entry corner-entry" bindtap="goEditProfile">
  <text class="profile-entry-title">{{hasProfile ? '修改资料' : '完善资料'}}</text>
</view>

<!-- 顶部用户信息 -->
<!-- <view class="top-user" wx:if="{{hasUserInfo}}">
  <image class="avatar" src="{{user.avatar}}" />
  <text class="name">{{user.nickName}}</text>
</view> -->

<!-- 授权遮罩 -->
<!-- <view class="auth-mask" wx:if="{{!hasUserInfo}}"> -->
  <!-- <view class="auth-card">
    <view class="title">欢迎来到答题挑战</view>
    <view class="desc">
      需要获取你的头像和昵称<br/>
      用于排行榜和对战展示
    </view>
    <button class="auth-btn" bindtap="getUserInfo">
      授权并进入
    </button>
  </view> -->
<!-- </view> -->

<!-- Logo -->
<!-- Logo -->
<image src="{{kid}}" class="kid-bg" mode="aspectFit" />
<image src="{{imgLogo}}" class="logo-img" mode="aspectFit" />

<!-- 功能入口 -->
<view class="menu" >

  <view class="menu-item" bindtap="goSingle">
    <image src="{{img1}}" class="menu-img" mode="aspectFit" />
  </view>

  <view class="menu-item" bindtap="goRoom">
    <image src="{{img2}}" class="menu-img" mode="aspectFit" />
  </view>

  <view class="menu-item" bindtap="goRank">
    <image src="{{img3}}" class="menu-img" mode="aspectFit" />
  </view>

</view>

</view>
